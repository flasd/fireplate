/* eslint-disable */

describe('Server / renderAppMiddleware', () => {
    it('should run without exploding');
    it('should render the app properly');
    it('should render the proper route for a given request url');
    it('should render a notFound route with 404 status when req.url doesnt exists');
});
